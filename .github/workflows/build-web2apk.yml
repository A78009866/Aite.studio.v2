# =============================================================================
# Aite.studio - Web to APK Builder (Intelligent v2.5 - Java 21 Fix)
# Fixed: Updated Java from 17 to 21 to support latest Capacitor Android
# =============================================================================

name: Build Web to APK

on:
  repository_dispatch:
    types: [build-web2apk]
  workflow_dispatch:
    inputs:
      app_name:
        description: 'App Name'
        required: true
        default: 'MyApp'
      package_name:
        description: 'Package Name'
        required: true
        default: 'com.example.app'
      icon_url:
        description: 'Icon URL'
        required: true
      zip_url:
        description: 'Project ZIP URL'
        required: true
      request_id:
        description: 'Build ID'
        required: false
        default: 'manual'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Clear Gradle Cache
        run: |
          echo "🧹 Clearing old Gradle cache..."
          rm -rf ~/.gradle/caches
          rm -rf ~/.gradle/wrapper

      - name: Download Project
        run: |
          mkdir -p project && cd project
          echo "📥 Downloading project archive..."
          curl -sL "${{ github.event.client_payload.zip_url || github.event.inputs.zip_url }}" -o project.zip

          echo "📦 Extracting archive..."
          unzip -q project.zip
          rm project.zip

          echo "📂 Initial structure:"
          ls -la

      - name: Intelligent Project Structure Detection
        id: detect
        run: |
          cd project
          shopt -s extglob

          echo "🔍 Analyzing project structure..."

          find_entry_point() {
            local search_dirs=("$@")
            for dir in "${search_dirs[@]}"; do
              if [ -f "$dir/index.html" ]; then echo "$dir/index.html"; return 0; fi
              if [ -f "$dir/index.htm" ]; then echo "$dir/index.htm"; return 0; fi
            done
            for dir in "${search_dirs[@]}"; do
              local index_file=$(find "$dir" -maxdepth 1 -type f \( -iname "*index*.html" -o -iname "*index*.htm" -o -iname "*home*.html" -o -iname "*main*.html" \) 2>/dev/null | head -1)
              if [ -n "$index_file" ]; then echo "$index_file"; return 0; fi
            done
            for dir in "${search_dirs[@]}"; do
              local first_html=$(find "$dir" -maxdepth 2 -type f \( -iname "*.html" -o -iname "*.htm" \) 2>/dev/null | head -1)
              if [ -n "$first_html" ]; then echo "$first_html"; return 0; fi
            done
            return 1
          }

          HTML_COUNT=$(find . -type f \( -name "*.html" -o -name "*.htm" \) 2>/dev/null | wc -l)
          SUBDIRS=$(find . -maxdepth 1 -type d 2>/dev/null | wc -l)

          echo "Found $HTML_COUNT HTML files"

          if [ "$HTML_COUNT" -eq 1 ] && ls *.html 1>/dev/null 2>&1; then
            echo "✅ Single HTML file"
            mkdir -p www
            cp -r !(www) www/ 2>/dev/null || cp -r ./* www/
            echo "entry_point=www/index.html" >> $GITHUB_OUTPUT
          elif [ -d "www" ] && [ -f "www/index.html" ]; then
            echo "✅ Pre-built www folder"
            echo "entry_point=www/index.html" >> $GITHUB_OUTPUT
          elif [ -d "dist" ] && [ -f "dist/index.html" ]; then
            echo "✅ dist folder"
            mkdir -p www && cp -r dist/* www/
            echo "entry_point=www/index.html" >> $GITHUB_OUTPUT
          elif [ -d "build" ] && [ -f "build/index.html" ]; then
            echo "✅ build folder"
            mkdir -p www && cp -r build/* www/
            echo "entry_point=www/index.html" >> $GITHUB_OUTPUT
          elif [ "$SUBDIRS" -eq 2 ] && [ -z "$(ls *.html 2>/dev/null)" ]; then
            NESTED_DIR=$(find . -maxdepth 1 -type d ! -path . | head -1)
            echo "📁 Nested: $NESTED_DIR"
            mkdir -p www
            cp -r "$NESTED_DIR"/* www/
            echo "entry_point=www/index.html" >> $GITHUB_OUTPUT
          elif [ -f "index.html" ]; then
            echo "✅ Flat structure"
            mkdir -p www
            cp -r !(www) www/ 2>/dev/null || cp -r ./* www/
            echo "entry_point=www/index.html" >> $GITHUB_OUTPUT
          else
            ENTRY=$(find_entry_point ".")
            if [ -n "$ENTRY" ]; then
              echo "✅ Entry: $ENTRY"
              mkdir -p www && cp -r !(www) www/ 2>/dev/null || cp -r ./* www/
              ENTRY_NAME=$(basename "$ENTRY")
              if [ "$ENTRY_NAME" != "index.html" ]; then
                (cd www && mv "$ENTRY_NAME" index.html 2>/dev/null || cp "$ENTRY_NAME" index.html)
              fi
              echo "entry_point=www/index.html" >> $GITHUB_OUTPUT
            else
              echo "❌ No HTML files!"
              exit 1
            fi
          fi

          ls -la www/

      - name: Download Icon
        run: |
          mkdir -p project/icon
          curl -sL "${{ github.event.client_payload.icon_url || github.event.inputs.icon_url }}" -o project/icon/icon.png || echo "⚠️ Icon download failed"

      - name: Setup Node.js 22
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      # FIX: Updated to Java 21
      - name: Setup Java 21
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '21'

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Build APK
        run: |
          echo "🚀 Starting build..."
          mkdir -p build && cd build

          # Initialize with Node 22
          npm init -y
          npm install @capacitor/core @capacitor/cli @capacitor/android

          # Create config
          cat > capacitor.config.json <<EOF
          {
            "appId": "${{ github.event.client_payload.package_name || github.event.inputs.package_name }}",
            "appName": "${{ github.event.client_payload.app_name || github.event.inputs.app_name }}",
            "webDir": "www"
          }
          EOF

          cp -r ../project/www ./

          # Add Android
          npx cap add android
          npx cap sync

          # Setup icon
          if [ -f "../project/icon/icon.png" ]; then
            mkdir -p android/app/src/main/res/mipmap-{hdpi,mdpi,xhdpi,xxhdpi,xxxhdpi}
            for dir in hdpi mdpi xhdpi xxhdpi xxxhdpi; do
              cp ../project/icon/icon.png android/app/src/main/res/mipmap-${dir}/ic_launcher.png
            done
          fi

          # Build
          cd android
          chmod +x gradlew
          ./gradlew assembleDebug --no-daemon \
            -Dorg.gradle.java.home=$JAVA_HOME \
            -Dfile.encoding=UTF-8

          echo "✅ Build complete!"

      - name: Sign APK
        run: |
          cd build/android
          APK=$(find app/build/outputs/apk -name "*.apk" -type f | head -1)
          [ -z "$APK" ] && { echo "❌ No APK found!"; exit 1; }

          keytool -genkey -v -keystore key.jks -alias key -storepass 123456 -keypass 123456 \
            -keyalg RSA -validity 10000 -dname "CN=Android,O=Android,C=US" 2>/dev/null

          jarsigner -keystore key.jks -storepass 123456 -keypass 123456 \
            -sigalg SHA256withRSA -digestalg SHA-256 "$APK" key

          cp "$APK" ../../app.apk
          echo "✅ Signed: app.apk"

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: build-${{ github.event.client_payload.request_id || github.event.inputs.request_id }}
          name: "${{ github.event.client_payload.app_name || github.event.inputs.app_name }}"
          files: app.apk
          fail_on_unmatched_files: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
